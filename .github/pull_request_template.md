## What changed
<one or two sentences>

## Verification
- [ ] `npm run build` passes locally.
- [ ] `/api/health` returns 200 (against local dev or deployed preview).
- [ ] I opened the changed surface in a browser under valid auth and clicked through the golden path.

## Notes for the reviewer
<anything the reviewer should look at first, screenshots, follow-ups, etc.>
